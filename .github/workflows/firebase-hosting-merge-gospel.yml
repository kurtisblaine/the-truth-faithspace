# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy - Gospel
on:
  push:
    branches:
      - master
    paths:
      - "apps/gospel/**"
jobs:
  build_and_deploy-gospel:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - with:
        lfs: true
      - run: npm install --force && npx nx build gospel --prod --output-path apps/gospel/public
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_BLOG_46974 }}"
          channelId: live
          projectId: blog-46974
          entryPoint: ./apps/gospel
